{% extends "layout.html" %}

{%  block titulo %}Galeria imagens{% endblock %}

{% block direita %}{% endblock %}
{% block esquerda %}{% endblock %}
{%block conteudo%}

<div id="conteudo_geral">
	<h2>Editar perfil</h2>

	
    <div class="aux_form">
        <a href="/perfil/perfil/"  class="select" ><span class="ui-icon ui-icon-person"></span>Perfil</a>
        <a href="/perfil/profissional/"><span class="ui-icon ui-icon-document"></span>Profissional</a>
        <a href="/perfil/contato/"><span class="ui-icon ui-icon-note"></span>Contatos</a>
        <a href="/perfil/redes/" ><span class="ui-icon ui-icon-refresh"></span>Redes Sociais</a>
    </div>
	<div id="formperfil">
		<form method="POST" enctype="multipart/form-data">
			 {% if formperfil %}{{formperfil.as_table}}{% endif %}
			 
			 <input type="submit" value="Savar alterações" ></input>
			
		</form>
	</div>
	
	<div id="instruções">
	    <img style="margin-left:20px" src="{{MEDIA_URL}}images_portal/perfil/manual_perfil.png">    
	</div>
	
	<div class="pefil_galeria">
	    {% if list_fotos %}

        {% for foto in list_fotos %}
				    <div class="galeria_full_perfil">
					    <div class="img_video_perfil">
						    <a class = "foto" href="{{ MEDIA_URL }}{{ foto.img_foto }}">
							    <img src="{{ MEDIA_URL }}{{ foto.img_miniatura }}" alt="{{ foto.vch_titulo }}" title="{{ imagem.vch_titulo }}" />
						    </a>
					    </div>

				    </div>

	    {% endfor %}
	    <a href="/perfil/album/" class="ver_mais">Adicionar mais</a>
	    {% else %}
	    <span> Não possui fotos no album. Para adicionar click <a href="/perfil/album/">aqui</a>  </span>
	    {% endif %}
	    
	</div>
	
    {% if perfil_list  %}
   
        <table id="perfil" width="100%" border="0" cellspacing="0" cellpadding="0">
            <thead>
                <th>Tipo</th>
                <th>Formação</th>
                <th>Instituição</th>
                <th>Local</th>
                <th>Ações</th>
            </thead>
            <tbody>
                {%for profissional in perfil_list%}
                <tr class="{%cycle 'linha1' 'linha2' %}" >
                    {%if profissional.tipo = "1" %}
                        <td> Profissional</td>
                    {%else%}
                        <td>Especialização</td>
                    {%endif%}
                    <td>{{profissional.formacao}}</td>
                    <td>{{profissional.instituicao}}</td>
                    <td>{{profissional.local}}</td>

                </tr>
                {%endfor%}
            </tbody>

        </table>
    
    {%endif%}
	
</div>
	{%endblock%}


	{%block includejs%}

	{% endblock %}

	{% block scriptjs%}

	{% endblock %}


