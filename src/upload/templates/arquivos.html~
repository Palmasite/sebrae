{% extends 'layout.html' %}
{%  block titulo %}Videos{% endblock %}

{% block direita %}{% endblock %}


{%block conteudo%}
<div id="conteudo_interno">
    {% for galeria in lista_galeria %}
        <h2>{{galeria.descricao}}</h2>
        <div class="galeria_upload">   

            {% for doc in lista_arquivo %}
                
                    {% if doc.5 == galeria.idgaleria %}  
                        <div class="arquivo">              
                            <a href="{{MEDIA_URL}}{{vch_arquivo}}"><img src="{{doc.4}}"/> </a>
                            <strong>Arquivo</strong> : {{doc.1}}<br/>
                            <strong>Resumo</strong> : {{doc.2}}</br>
                            <strong>Galeria</strong> : {{galeria.descricao}}<br/>
                            <strong>Data</strong> : {{doc.data}}
                        </div>
                     {%endif%}

            {%endfor%}
        </div>
    {% endfor %}
</div>
{% endblock %}
